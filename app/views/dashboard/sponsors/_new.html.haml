.modal-dialog
  .modal-content
    = form_for(@sponsor, url: dashboard_sponsors_path, remote: true, html:{class: :"form-horizontal parsley-form"}) do |f|
      .modal-header
        %button.close{type: :"button", aria:{hidden: true}, data:{dismiss: :modal}} ×
        %h4.modal-title#data_title Add Sponsor

      .modal-body
        .form-group
          = f.label :name, "Sponsor Name :", class: :"col-sm-4 control-label"
          .col-sm-6
            = f.text_field :name, placeholder: "e.g. SPONSOR", required: true, class: :"form-control input-sm"

      .modal-footer
        = button_tag(type: :submit, class: :"btn btn-primary") do
          Save
          %i.fa.fa-angle-double-right
        %button.btn.btn-default{type: :button, data:{dismiss: :modal}} Cancel