- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class=>("alert alert-#{(name == "notice" or name == "success") ? "success" : "danger"}") } 
      %a.close{:"data-dismiss"=>"alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
